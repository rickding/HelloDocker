version: "3"
services:
    web:
        image: "java-app"
        build: .
        command: "java Hello"

        restart: always
        working_dir: /usr/code/
        volumes:
            - ./:/usr/code/
